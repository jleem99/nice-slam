[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "nice-slam"
version = "0.1.0"
description = "NICE-SLAM: Neural Implicit Scalable Encoding for SLAM"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["src*"]
